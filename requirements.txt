# ZEB-ROI Consulting Chatbot — Python 패키지 의존성
# Python 3.10+ 권장
# 설치: pip install -r requirements.txt

# === Web UI ===
streamlit>=1.30.0

# === LLM API ===
anthropic>=0.39.0          # Claude Haiku 4.5
openai>=1.50.0             # 임베딩 전용 (text-embedding-3-small)

# === RAG (모드 1, 정책 Q&A) ===
# 검색기 = core.rag_retriever.KeywordRetriever — ChromaDB에서 청크 '원문'만 꺼내
# TF-IDF(한글 char-bigram)로 매칭한다. 저장된 임베딩 벡터를 조회하지 않는다.
#   → 임베더는 런타임에 불필요하고, 인덱스 빌드 때도 필요 없다
#     (build_index.py 기본값 --provider hash: 의존성·API키·네트워크 0).
#   → 무료 클라우드 티어(~1GB)에서도 안정 동작.
# (선택) 나중에 벡터 검색으로 전환할 때만: pip install fastembed   # 다국어 ONNX
# llama-index 제거 — 코드에서 import 0건인 죽은 의존성.
chromadb>=0.4.20
protobuf==3.20.3

# === 데이터 처리 ===
pandas>=2.0.0
openpyxl>=3.1.0            # 엑셀 (07 단가DB, 08 간접비)
pypdf>=4.0.0               # PDF 백업 파서

# === 환경 변수 ===
python-dotenv>=1.0.0

# === 유틸 ===
tenacity>=8.2.0            # API 재시도 로직
reportlab>=4.0.0           # PDF 진단 리포트 생성
requests>=2.31.0           # core/eplus_client.py — EnergyPlus 서비스 호출
                           # streamlit이 끌고 오지만 transitive에 기대면 조용히 깨진다.

# gbXML 파서(core/gbxml_parser.py)·IDF 생성기(core/idf_writer.py)는 표준 라이브러리만 쓴다.
#   · openstudio(78.7MB)는 wheel이 cp313까지라 Python 3.14에 설치 불가.
#     게다가 그 gbXML 임포트가 Construction을 조용히 누락시키는 알려진 결함이 있다.
#   · eppy는 시뮬레이터가 아니라 IDF 편집기이고, 동봉 IDD 최신이 V9_2_0(2019)이라
#     현행 E+ 26.1보다 낡았다. 우리는 IDF를 생성만 하므로 텍스트 출력이면 충분하다.
# EnergyPlus 자체는 별도 Docker 서비스에서 돈다 → energyplus_service/README.md


# 파라미터 테이블(data/params/*.yaml) 로더 — core/params.py
PyYAML>=6.0

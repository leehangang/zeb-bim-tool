# =====================================================
# 절대 GitHub에 올리면 안 되는 것 — API 키
# =====================================================
.env
.env.local
.env.*.local

# =====================================================
# 자동 생성 파일 — 굳이 올릴 필요 없음
# =====================================================
data/chroma_db/
!data/chroma_db.zip  # 배포용 인덱스는 zip으로 압축해 푸시
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# =====================================================
# 가상환경
# =====================================================
venv/
env/
ENV/
.venv/

# =====================================================
# IDE 설정
# =====================================================
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# =====================================================
# Streamlit
# =====================================================
.streamlit/secrets.toml

# =====================================================
# 캐시
# =====================================================
.pytest_cache/
.cache/
*.log

# =====================================================
# 정책 자료 PDF — GitHub에 안 올리는 게 안전
# (저작권 문제는 없으나 용량 절약)
# =====================================================
# data/policy_docs/*.pdf
# data/policy_docs/*.xlsx
# → 주석 해제 시 PDF/엑셀이 git에서 제외됨
#   민회님 본인 PC에서만 작업할 거면 위 주석 풀기 권장

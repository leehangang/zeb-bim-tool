이 폴더에 아래 파일명 그대로 사진을 저장하면, presentation.html 슬라이드에
자동으로 표시됩니다 (점선 자리표시가 사진으로 바뀜). 파일이 없으면 점선 안내가 보입니다.

필요한 사진 (총 5장) — 가로 사진 권장(16:9 또는 4:3):

[필수]
1. app-bim.jpg       — 앱 'BIM 진단+ZEB 평가' 결과 화면 (점수·등급)        → 16p 실제 화면
3. app-roi.jpg       — 앱 'ROI 시뮬레이션' 답변 화면                       → 16p 실제 화면
4. app-rag.jpg       — 앱 '정책 Q&A' 답변+출처 카드 화면                   → 16p 실제 화면
5. app-intake.jpg    — 앱 '사업 신청 인테이크' 대화·진행률 화면            → 16p 실제 화면

[있으면 좋음]
6. bim-model.jpg     — Revit/Dynamo BIM 3D 모델 화면                       → 4p 배경(BIM)

팁:
- 앱 화면(2~5)은 https://zeb-bim-tool.streamlit.app 에서 각 모드 실행 후 캡처.
- 가로로 길게 잘라 찍으면 칸(4:3 비율 셀)에 깔끔하게 들어갑니다.
- 파일이 세로로 길어도 자동으로 셀에 맞춰 잘려 들어갑니다(crop).

⚠️ 건물 외관 사진은 쓰지 않는다 — 검증 표본은 익명 처리 대상이고,
   외관은 이름을 지워도 건물을 특정한다. 실증 근거는 BIM 모델로 보인다.
